# MLflow project definition
name: simple-llm-pipeline

conda_env: conda.yaml

entry_points:
    main:
        parameters:
          question: {type: str}
        command: "python run.py --question {question}"